# Core dependency: the biotuner resonance engine.
# We track biotuner `main` directly during active development (the resonance +
# n:m soundness work this suite uses lives on main, and the PyPI 0.4.1 release is
# stale). Caveat: biotuner.__version__ still reads 0.4.1 on main, so pip can't tell
# when main moves — refresh with `pip install --force-reinstall --no-deps \
# "biotuner @ git+https://github.com/AntoineBellemare/biotuner.git@main"`, or use an
# editable local clone (`pip install -e <path-to-biotuner>`).
# FOR A PUBLISHED / ARCHIVAL BUILD: replace `@main` below with an immutable commit
# SHA so the paper's results are exactly reproducible (last known-good: cd415cb).
biotuner @ git+https://github.com/AntoineBellemare/biotuner.git@main

# Scientific stack
numpy>=1.23
scipy>=1.9
matplotlib>=3.6
joblib>=1.2
scikit-learn>=1.1

# Real-data acquisition / simulation
mne>=1.4            # PhysioNet EEG (eegbci) auto-download
neurokit2>=0.2      # ECG / PPG / RSP simulation

# Paper compilation (optional): the eLife-style manuscript is built with
# resonance_paper/paper/build_elife_tex.py (stdlib only) and compiled with
# `tectonic` (a self-contained LaTeX engine; install separately, not via pip).
