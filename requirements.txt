# Core dependency: the biotuner resonance engine.
# IMPORTANT: the resonance fixes used by this suite (phase-row alignment,
# factor-level surrogate z) landed on biotuner `main` AFTER the v0.4.1 PyPI
# release and the version was NOT bumped — so `pip install biotuner` (0.4.1)
# does NOT reproduce these results, and `biotuner.__version__` reads 0.4.1 even
# on main (so the version string cannot distinguish them). We therefore PIN an
# immutable commit (main HEAD, 2026-06-28). This SHA adds the n:m soundness work:
# canonical (Tass-convention) coupling metrics, the all-moment indices
# (nm_rho_entropy / nm_conditional_prob / nm_phase_mi), the cross-signal
# detect_nm_coupling detector, and the peak-coupling defaults (bandwidth 3 Hz,
# aggregate=max) these studies rely on. For active biotuner development, swap the
# SHA below back to `@main`.
biotuner @ git+https://github.com/AntoineBellemare/biotuner.git@cd415cba933da4950f26879556c05e305445a023

# Scientific stack
numpy>=1.23
scipy>=1.9
matplotlib>=3.6
joblib>=1.2
scikit-learn>=1.1

# Real-data acquisition / simulation
mne>=1.4            # PhysioNet EEG (eegbci) auto-download
neurokit2>=0.2      # ECG / PPG / RSP simulation

# Paper compilation (optional): the eLife-style manuscript is built with
# resonance_paper/paper/build_elife_tex.py (stdlib only) and compiled with
# `tectonic` (a self-contained LaTeX engine; install separately, not via pip).
